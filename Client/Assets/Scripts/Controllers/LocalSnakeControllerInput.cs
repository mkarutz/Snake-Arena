using UnityEngine;
using System.Collections;

public class LocalSnakeControllerInput : LocalSnakeController {

    /*
	override protected void MovementControl()
    {
        SnakeState state = this.GetComponent<SnakeState>();
        Vector3 lookVec = (Input.mousePosition - new Vector3(Screen.width, Screen.height, 0.0f) * 0.5f);

        this.SetTargetDirection(lookVec);
    }*/
}
